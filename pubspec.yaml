name: gastoapk
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_iconpicker: ^3.0.1 # Adicionado aqui
  flutter_colorpicker: ^1.0.3 # Adicionado aqui
  flutter_localizations: # Adicionado aqui
    sdk: flutter
  pie_chart: ^5.0.0 # Adicionado aqui
  flutter_svg: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - lib\logotipo\gasto_guard_logo.svg
